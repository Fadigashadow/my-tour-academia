import { ParseGenderPipe } from './parse-gender.pipe';

describe('ParseGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
