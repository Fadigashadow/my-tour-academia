import { OppositeGenderPipe } from './opposite-gender.pipe';

describe('OppositeGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new OppositeGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
