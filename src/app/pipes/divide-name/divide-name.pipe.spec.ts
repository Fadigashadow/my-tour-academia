import { DivideNamePipe } from './divide-name.pipe';

describe('DivideNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DivideNamePipe();
    expect(pipe).toBeTruthy();
  });
});
