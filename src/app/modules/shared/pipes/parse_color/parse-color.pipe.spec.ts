import { ParseColorPipe } from './parse-color.pipe';

describe('ParseColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseColorPipe();
    expect(pipe).toBeTruthy();
  });
});
